---
interface Props {
  index?: string
  label: string
  class?: string
}
const { index, label, class: className } = Astro.props
---

<p class:list={['font-mono text-xs tracking-wide text-vernaillen-500', className]}>
  <span aria-hidden="true">//</span> {index ? `${index} — ` : ''}{label}
</p>
