---
import Eyebrow from './Eyebrow.astro'

interface Props {
  index?: string
  label: string
  class?: string
}
const { index, label, class: className } = Astro.props
---

<div class:list={['lg:sticky lg:top-20', className]}>
  <Eyebrow index={index} label={label} />
  <slot />
</div>
