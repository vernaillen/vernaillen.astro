---
import Eyebrow from './Eyebrow.astro'

interface Props {
  label: string
  class?: string
}
const { label, class: className } = Astro.props
---

<div class:list={[className]}>
  <Eyebrow label={label} />
  <slot />
</div>
